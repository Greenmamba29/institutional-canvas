# LithiumBuy Enterprise - Gap Analysis Implementation Plan

This plan is based on the **Production Readiness Gap Analysis** (January 13, 2026) which identified a B- grade foundation requiring 4-6 weeks of focused backend and integration development.

## 🎯 Strategic Objective
Transition from a "Frontend-Only" MVP to a secure, scalable, and production-ready B2B marketplace.

---

## 🏗️ Phase 1: Security & Infrastructure (High Priority - P0)
**Estimated Time: 2 Weeks**

### 1.1 Authentication & Multi-Tenancy
- [ ] **Implement Auth Flow**: Replace Passport.js placeholders with full implementation linked to Supabase Auth.
- [ ] **RLS Completion**: Draft and apply RLS policies for all 52 tables (specifically `quotes`, `orders`, `user_profiles`).
- [ ] **JWT Integration**: Ensure `jwt_org_id()` and `current_sub()` are correctly utilized in all RPCs.

### 1.2 Database & Data Layer
- [ ] **Drizzle Integration**: Replace hardcoded `suppliers.ts` data with real PostgreSQL queries.
- [ ] **Transaction Logic**: Implement transaction wrapping for complex operations (e.g., `create_deal`).
- [ ] **Env Validation**: Add `dotenv` validation on startup to fail-fast if `GEMINI_API_KEY` or `DAILY_API_KEY` is missing.

### 1.3 Security Hardening
- [ ] **Rate Limiting**: Add `express-rate-limit` to API routes.
- [ ] **Middleware**: Add `csurf` and full `Helmet.js` configuration.
- [ ] **Error Sanitization**: Ensure stack traces are hidden in production.

---

## 🚀 Phase 2: Service Integration (P1)
**Estimated Time: 2 Weeks**

### 2.1 TeleBuy & Contracts
- [ ] **Daily.co Integration**: Replace `videoService.ts` placeholders with real Daily.co API calls.
- [ ] **DocuSign Flow**: Wire the contract generation UI to real DocuSign templates.
- [ ] **Frontend Video**: Integrate `@daily-co/react` for the in-app meeting experience.

### 2.2 AI & Market Intelligence
- [ ] **Gemini 2.5 Flash**: Connect the AI Studio to the real Google Generative AI SDK.
- [ ] **Circuit Breakers**: Implement `opossum` or similar for all external API calls.
- [ ] **Job Queues**: Set up BullMQ/Redis for async email and data sync tasks.

---

## 🧪 Phase 3: Testing & Quality (P2)
**Estimated Time: 1 Week**

### 3.1 Backend Testing
- [ ] **Integration Tests**: 80%+ coverage for all server routes in `server/routes.ts`.
- [ ] **Health Checks**: Expand `/health` to include downstream dependency checks.

### 3.2 Frontend Refinement
- [ ] **Loading States**: Implement Skeleton screens for all data-heavy components.
- [ ] **Memory Management**: Fix `URL.revokeObjectURL()` leaks in `AIStudio.tsx`.
- [ ] **Virtualization**: Implement `react-window` for large supplier/RFQ lists.

---

## 📊 Success Metrics
| Criteria | Target |
|----------|--------|
| **Mock Data** | 0% remaining |
| **API Coverage** | 100% of routes implemented |
| **Test Coverage** | >80% for backend |
| **Security Audit** | Pass (No critical vulnerabilities) |
| **Load Time** | < 2s for dashboard |

---

## 🛠️ Immediate Actions (Next 48 Hours)
1. **Sync Env**: Copy `.env.example` to `.env` and procure development keys for Daily.co and Gemini.
2. **Route Skeleton**: Begin implementing the `GET /api/suppliers` route using real database data.
3. **RLS Audit**: Run the `VERIFICATION_TESTS.sql` to identify policy gaps.

**Generated by LithiumBuy AI Assistant**  
*January 13, 2026*
